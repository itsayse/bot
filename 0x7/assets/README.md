Drop your welcome image here as `welcome.png`.

The Welcome feature (configurable via `/panel → Welcome`) will automatically
attach it to the welcome embed for new members, as long as "Use Image" is
enabled and this file exists. If it's missing, welcome messages still send
fine — just without the image.
